# Walks through directories and gathers raw data.
#
# What id does:
#
# - Walks through the directories and gathers raw data.
# - Applies ignore rules.
# - Collect file sizes and paths.
# - Detect duplicate files (optional).
# - Count extensions.

from pathlib import Path

def scan():
    '''
    Walks through the directories and gathers raw data.
    '''
    # Placeholder for scanning logic.
    # This would include walking through directories, applying ignore rules,
    # collecting file sizes and paths, detecting duplicates, and counting extensions.
    pass
